#    ArchUpd
> **Safe Update for Arch Linux**

#    Система безопасного обновления Arch Linux с установщиком и таймером запуска
###    Version: v.3.0-2026_ru final     

Набор инструментов для безопасного, автоматизированного и удобного обновления Arch Linux. Система включает интерактивный менеджер, фоновую службу самообслуживания и  базовый инсталлятор. В версии 3.0 собраны лучшие наработки, ранее доступные в code-sketches/src/scripts/arch_update, а также оптимизирована установка и настройка.

---
**Дисклеймер:** Программное обеспечение предоставляется "как есть". Автор не несет ответственности за сохранность ваших данных. Всегда проверяйте работоспособность бэкапов Timeshift. Если Ваша система тонко настроена под некоторые стандарты, этот апдейтер может изменить некоторые настройки. Проверьте установщик и скрипты archupd, чтобы убедиться, что настройки установщика и апдейтера не изменяют важные для вас параметры.
---

## 🚀 Ключевые отличия ArchUpd от аналогов

* **Многофакторная проверка безопасности:** Скрипт выполняет комплексные проверки перед запуском:
  - Свободное место в `/` и `/boot` (настраиваемые пороги)
  - Сетевое подключение (HTTPS к зеркалам)
  - Источник питания (для ноутбуков)
  - Отсутствие блокировок pacman

* **Защищенный /boot с верификацией ядра:** 
  - `/boot` по умолчанию находится в режиме Read-Only
  - Переводится в Read-Write только на время обновления ядра
  - После обновления выполняется глубокая проверка образа `vmlinuz`
  - Автоматический ремонт при обнаружении проблем

* **Двухуровневое резервирование:**
  - Автоматические снапшоты Timeshift
  - Rsync-копии критических конфигов из `~/.config/`
  - Сохранение списка всех пакетов (`pkglist.txt`)
  - Быстрый режим `--no-backup` для экстренных обновлений

* **Интеллектуальная автоматизация:**
  - Интерактивный режим с предварительным расчетом (`--dry-run`)
  - Фоновое обновление с лимитом трафика
  - Самолечение (автообновление `archlinux-keyring`)
  - Уведомления через DBus

---

## 🛠 Компоненты системы

### 1. `archupd` (Полная ручная версия)
Основной интерактивный инструмент для управления обновлениями.
*   **Снимки системы:** Автоматическое создание Snapshot через `Timeshift` перед изменениями.
*   **Резервное копирование:** Синхронизация критических папок `~/.config/` и сохранение списка пакетов `pkglist.txt` в `~/Documents/Arch_Backups`.
*   **Умная проверка (Dry-Run):** Расчет суммарного веса обновлений в МБ и вывод списка изменений без прав root.
*   **Интеллектуальный чек:** Режим `--check` мгновенно показывает количество обновлений и статус питания, предлагая перейти к детальному расчету.
*   **Защита ядра:** Верификация формата `vmlinuz` после записи и автоматический ремонт в случае сбоя.
*   **Эскалация прав:** Точечное использование `sudo` только там, где это необходимо, и запуск AUR-хелперов от имени пользователя.

### 2. `archupdauto` (Фоновая Lite-версия)
Скрипт для автономного обслуживания системы без участия пользователя.
*   **Расписание:** Работает по таймеру `systemd` (раз в сутки с рандомной задержкой).
*   **Лимиты трафика:** Проверяет объем обновлений. Если размер превышает `MAX_DOWNLOAD_SIZE`, обновление откладывается.
*   **Самолечение:** Превентивное обновление `archlinux-keyring` для предотвращения ошибок подписи (PGP).
*   **Безопасный /boot:** Переводит раздел в Read-Write только на время установки ядра, делает `sync` и возвращает в Read-Only.
*   **Уведомления:** При успешном завершении отправляет пуш-уведомление на рабочий стол пользователя через `DBus`.

### 3. Служба и Таймер (`systemd`)
*   **`archupdauto.service`:** Облегченный юнит, запускающий обновление. Настроен на автоматический запуск уведомлений при успехе.
*   **`archupdauto.timer`:** Управляет периодичностью. Использует `RandomizedDelaySec`, чтобы не нагружать зеркала одновременно со всей сетью.

---

## ⚙️ Конфигурация (`/etc/archupd.conf`)

Единый файл настроек для всех компонентов:

| Переменная | Описание |
| :--- | :--- |
| `KERNEL_NAME` | Имя вашего ядра (`linux`, `linux-lts`, `linux-zen`) для верификации. |
| `KEEP_CACHE` | Количество версий пакетов, оставляемых в кэше (paccache). |
| `CHECK_NETWORK` | Включение/выключение проверки связи через HTTPS (Google/Yandex). |
| `MIN_FREE_ROOT` | Минимальный порог места на `/` (в МБ) для начала обновления. |
| `MAX_DOWNLOAD_SIZE` | Лимит веса обновлений (в МБ) для фонового режима. |
| `BACKUP_DIRS` | Массив папок из `~/.config/` для резервного копирования. |

---

## 📦 Установка и удаление

Скрипт `install.sh` проводит полную подготовку системы:
1.  **Зависимости:** Устанавливает `timeshift`, `rsync`, `pacman-contrib`, `curl`, `libnotify`.
2.  **Оптимизация:** Включает `ParallelDownloads` в `pacman.conf` и многопоточную сборку в `makepkg.conf`.
3.  **Безопасность:** Создает `/etc/sudoers.d/archupd` для работы `--dry-run` без пароля и применяет Hardening ядра (`SysRq = 244`).
4.  **Права:** Добавляет пользователя в группу `systemd-journal` для чтения логов без sudo.

Если Вы загрузили archupd в архиве `.tar.gz` или через `git clone`, скрипты имеют оригинальные права, 
в ином случае добавьте права на исполнение, затем просто запустите установку.

```bash
sudo chmod +x install.sh
sudo ./install.sh
```

Удаление — полная очистка системы от всех компонентов ArchUpd:

```bash
sudo ./install.sh --uninstall
```
### Установка через Git (рекомендуется)

```bash
git clone https://github.com/leoikt/ArchUpd.git
cd archupd
sudo ./install.sh
```

Примечание: После первой установки рекомендуется перезайти в систему для обновления прав групп пользователей.

---

## ⌨️ Быстрые команды

| Команда | Описание |
|---------|----------|
| `archupd` | Полный цикл обновления с резервным копированием |
| `archupd --help` | Показать справку по всем опциям |
| `archupd -c` | Быстрая проверка обновлений (без sudo) |
| `archupd --dry-run` | Расчет объема загрузки и просмотр изменений |
| `archupd -l` | Просмотр логов фонового сервиса |
| `archupd --verify` | Проверка и восстановление ядра |
| `archupd --noconfirm --no-backup` | Быстрое обновление без вопросов |
| `archupd --auto` | Запуск фоновой службы вручную |

**Примеры:**
```bash
# Проверить, затем обновить
archupd -c && archupd

# Экстренное обновление
archupd --noconfirm --no-backup

# Только расчет
archupd --check --dry-run
```

---

## ⚙️ Управление системой

### Фоновое обновление
* `sudo systemctl start archupd.service` — запустить немедленно.
* `systemctl status archupd.service`  — проверить результат.

### Таймер
* `systemctl list-timers archupd.timer` — когда следующий запуск.

### Уведомления
* `systemctl status archupd-notify@$USER.service` — статус службы пушей.

---

## ❓ Возможные вопросы

**Q: Скрипт требует пароль слишком часто**
A: Убедитесь, что установщик создал `/etc/sudoers.d/archupd` с правильными правами.

**Q: Timeshift не создает снапшоты**
A: Проверьте настройки Timeshift: `sudo timeshift --list-snapshots`

**Q: Как отключить автообновление?**
A: `sudo systemctl disable --now archupdauto.timer`

---

## 🧪 Тестирование и Обратная связь

Проект развивается и тестируется на различных конфигурациях. Ваша обратная связь поможет сделать ArchUpd стабильнее.

### Сообщить об ошибке
Если вы столкнулись с проблемой, пожалуйста, создайте тикет в разделе **[Issues](https://github.com/leoikt/ArchUpd.git)**. 

> **Важно:** Используйте предложенный шаблон баг-репорта. Это поможет быстрее диагностировать проблему, так как нам понадобятся данные о вашем ядре и логи работы скрипта (`archupd -l`).

### Внести вклад (Contributing)
1. Сделайте Fork репозитория.
2. Создайте ветку для своей функции (`git checkout -b feature/AmazingFeature`).
3. Сделайте Commit своих изменений (`git commit -m 'Add some AmazingFeature'`).
4. Сделайте Push в ветку (`git push origin feature/AmazingFeature`).
5. Откройте Pull Request.

### Протестировано на:
* **OS:** Arch Linux (x86_64)
* **WM/DE:** Hyprland, KDE Plasma, kitty, code-oss
* **Ядра:** `linux`, `linux-lts`
* **AUR-хелперы:** `yay`, `paru`
* **Hardware:** Acer Swift 3 [1165g7, 16 gb lpddr4x, Intel Xe, NVMe 1.4
* **Прочее:** SecureBoot, `/boot` в read-only, конфигурация безопасности, предлагаемая archupd дефолтно. 

---
